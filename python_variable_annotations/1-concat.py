#!/bin/usr/env python3
"""
A type-annoted function concat that
takes two strings as arguments and returns it concatenated
"""


def concat(a: str, b: str) -> str:
    """Return a concatenated string"""
    return f"{a}{b}"
