#!/usr/bin/env python3

"""
A type annotated function floor
Takes a float n as argument
Returns the floor of the float
"""
import math


def floor(n: float) -> int:
    """Returns the float as int"""
    return (math.floor(n))
